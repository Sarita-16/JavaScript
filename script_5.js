// 'let'  &  'const' are store in a different memory space 
// we can not access 'let'  &  'const' declaration before we have put some value in them 

// SyntaxError - TypeError - ReferenceError


let a = 10;
console.log(x);

const x = 25; // When we inatialize 'const' it expects initialization in the same line

var b = 100;