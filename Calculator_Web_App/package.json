{
  "name": "calculator_web_app",
  "version": "1.0.0",
  "description": "Calculator web app",
  "main": "calculator.js",
  "scripts": {
    "build": "build calculator.js --bundle --outfile=dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sarita Mollah",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.11"
  }
}
